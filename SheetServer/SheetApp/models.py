from django.db import models
from SheetApp import SheetConvert as SC
# Create your models here.
#class Buyers(models.Model):
#    for index, row in SC.retrieveAndOutput().iterrows():
    #    name =row['Name']
    #    phone = row['Phone']
    #    email = row['Email']
    #    address = row['Address']
